class Cyclone {
private:
    int a, b, c, d;
    int e, f, g, h;
    int i, j, k, l;
    int m, n, o, p;
    int q, r, s, t;
    int u;

public:
    void doit(int a, int b) // 11
    {
        switch (a) {
        case 1: // 1
            break;
        case 2: // 2
            break;
        case 3: // 3
            break;
        default:
            break;
        }
        if (a || b) // 4, 5
        {
        }
        if ((a || b) && (a && b)) // 6, 7, 8, 9
        {
        }
        do // 10
        {
        } while (0);
    }
};
